services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: zetesis
      POSTGRES_USER: zetesis
      POSTGRES_PASSWORD: zetesis_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
