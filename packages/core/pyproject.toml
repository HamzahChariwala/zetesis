[project]
name = "zetesis-core"
version = "0.1.0"
description = "Shared domain models and interfaces for Zetesis"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]
